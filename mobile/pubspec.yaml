name: globalerrands_mobile
description: GlobalErrands Flutter starter
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  flutter_secure_storage: ^8.0.0
